{% comment %}
  Template Contact - Page de contact pour recevoir les emails de devis
{% endcomment %}

{% assign page_title = 'Contact - Devis Néon LED Personnalisé | LumiNéon' %}
{% assign page_description = 'Contactez LumiNéon pour votre projet de néon LED personnalisé. Devis gratuit sous 24h, mockup 3D offert. Fabrication française haut de gamme, garantie 2 ans.' %}

<div class="min-h-screen bg-gray-900 text-white py-20">
  <!-- SEO Schema -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "ContactPage",
    "name": "{{ page_title }}",
    "description": "{{ page_description | escape }}",
    "url": "{{ shop.url }}/pages/contact",
    "mainEntity": {
      "@type": "LocalBusiness",
      "name": "LumiNéon",
      "telephone": "+33 1 23 45 67 89",
      "email": "lumineonshop@gmail.com",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "123 Rue de la Lumière",
        "addressLocality": "Paris",
        "postalCode": "75001",
        "addressCountry": "FR"
      }
    }
  }
  </script>

  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Breadcrumb -->
    <nav class="mb-8" aria-label="Fil d'Ariane">
      <ol class="flex items-center space-x-2 text-sm text-gray-400">
        <li><a href="/" class="hover:text-cyan-400 transition-colors">Accueil</a></li>
        <li><span class="mx-2">/</span></li>
        <li class="text-white">Contact</li>
      </ol>
    </nav>

    <!-- Header -->
    <div class="text-center mb-12">
      <h1 class="text-4xl sm:text-5xl font-bold mb-6" style="background: linear-gradient(45deg, #00f5ff, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
        Contactez-Nous
      </h1>
      <p class="text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed">
        🎨 Une question sur votre projet néon ? Notre équipe d'experts vous répond sous 24h avec un devis gratuit et un mockup 3D !
      </p>
    </div>

    {% if form.posted_successfully? %}
      <!-- Success Message -->
      <div class="bg-gradient-to-r from-green-500/20 to-emerald-500/20 border border-green-500/30 rounded-2xl p-8 mb-8 text-center">
        <div class="w-16 h-16 bg-gradient-to-r from-green-500 to-emerald-600 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        <h3 class="text-2xl font-bold text-green-400 mb-4">📧 Message Envoyé !</h3>
        <p class="text-green-300">
          Merci pour votre message ! Notre équipe vous recontactera sous 24h avec votre devis personnalisé.
        </p>
      </div>
    {% endif %}

    {% if form.errors %}
      <!-- Error Messages -->
      <div class="bg-gradient-to-r from-red-500/20 to-pink-500/20 border border-red-500/30 rounded-2xl p-6 mb-8">
        <h3 class="text-lg font-semibold text-red-400 mb-3">❌ Erreurs dans le formulaire :</h3>
        <ul class="space-y-1 text-red-300">
          {% for field in form.errors %}
            <li>• {{ field | capitalize }} {{ form.errors.messages[field] }}</li>
          {% endfor %}
        </ul>
      </div>
    {% endif %}

    <div class="grid lg:grid-cols-2 gap-12">
      
      <!-- Contact Form -->
      <div class="bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl p-8 border border-gray-700">
        <h2 class="text-2xl font-bold mb-6 text-cyan-400">📝 Demande de Devis</h2>
        
        {% form 'contact' %}
          <div class="space-y-6">
            
            <!-- Name -->
            <div>
              <label for="contact-name" class="block text-sm font-medium text-gray-300 mb-2">
                Nom complet *
              </label>
              <input 
                type="text" 
                id="contact-name"
                name="contact[name]" 
                value="{{ form.name }}"
                required
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300"
                placeholder="Votre nom et prénom"
              />
            </div>

            <!-- Email -->
            <div>
              <label for="contact-email" class="block text-sm font-medium text-gray-300 mb-2">
                Email *
              </label>
              <input 
                type="email" 
                id="contact-email"
                name="contact[email]" 
                value="{{ form.email }}"
                required
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300"
                placeholder="votre@email.com"
              />
            </div>

            <!-- Phone -->
            <div>
              <label for="contact-phone" class="block text-sm font-medium text-gray-300 mb-2">
                Téléphone
              </label>
              <input 
                type="tel" 
                id="contact-phone"
                name="contact[phone]" 
                value="{{ form.phone }}"
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300"
                placeholder="+33 1 23 45 67 89"
              />
            </div>

            <!-- Project Type -->
            <div>
              <label for="contact-project" class="block text-sm font-medium text-gray-300 mb-2">
                Type de projet *
              </label>
              <select 
                id="contact-project"
                name="contact[project]" 
                required
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300"
              >
                <option value="">Sélectionnez votre projet</option>
                <option value="enseigne" {% if form.project == 'enseigne' %}selected{% endif %}>Enseigne commerciale</option>
                <option value="decoration" {% if form.project == 'decoration' %}selected{% endif %}>Décoration intérieure</option>
                <option value="evenement" {% if form.project == 'evenement' %}selected{% endif %}>Événementiel</option>
                <option value="autre" {% if form.project == 'autre' %}selected{% endif %}>Autre projet</option>
              </select>
            </div>

            <!-- Dimensions -->
            <div>
              <label for="contact-dimensions" class="block text-sm font-medium text-gray-300 mb-2">
                Dimensions souhaitées
              </label>
              <input 
                type="text" 
                id="contact-dimensions"
                name="contact[dimensions]" 
                value="{{ form.dimensions }}"
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300"
                placeholder="Ex: 50x20 cm"
              />
            </div>

            <!-- Message -->
            <div>
              <label for="contact-message" class="block text-sm font-medium text-gray-300 mb-2">
                Décrivez votre projet *
              </label>
              <textarea 
                id="contact-message"
                name="contact[body]" 
                required
                rows="5"
                class="w-full px-4 py-3 bg-gray-700/50 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:border-cyan-400 focus:ring-2 focus:ring-cyan-400/20 transition-all duration-300 resize-none"
                placeholder="Décrivez votre projet : texte souhaité, couleurs, budget approximatif, délais..."
              >{{ form.body }}</textarea>
            </div>

            <!-- Submit Button -->
            <button 
              type="submit"
              class="w-full flex items-center justify-center gap-3 px-8 py-4 bg-gradient-to-r from-cyan-500 to-purple-600 rounded-lg text-white font-semibold text-lg transition-all duration-300 hover:scale-105 hover:shadow-neon-cyan"
            >
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
              </svg>
              Envoyer ma Demande
            </button>

            <p class="text-xs text-gray-400 text-center">
              * Champs obligatoires - Réponse garantie sous 24h
            </p>
          </div>
        {% endform %}
      </div>

      <!-- Contact Info -->
      <div class="space-y-8">
        
        <!-- Contact Details -->
        <div class="bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl p-8 border border-gray-700">
          <h3 class="text-2xl font-bold mb-6 text-purple-400">📞 Nos Coordonnées</h3>
          
          <div class="space-y-4">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-emerald-600 rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                </svg>
              </div>
              <div>
                <div class="text-white font-semibold">Téléphone</div>
                <div class="text-gray-300">+33 1 23 45 67 89</div>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-cyan-600 rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
              </div>
              <div>
                <div class="text-white font-semibold">Email</div>
                <div class="text-gray-300">lumineonshop@gmail.com</div>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-pink-600 rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
              </div>
              <div>
                <div class="text-white font-semibold">Adresse</div>
                <div class="text-gray-300">123 Rue de la Lumière<br>75001 Paris, France</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Business Hours -->
        <div class="bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl p-8 border border-gray-700">
          <h3 class="text-2xl font-bold mb-6 text-yellow-400">🕐 Horaires</h3>
          
          <div class="space-y-3">
            <div class="flex justify-between">
              <span class="text-gray-300">Lundi - Vendredi</span>
              <span class="text-white font-semibold">09:00 - 18:00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-300">Samedi</span>
              <span class="text-white font-semibold">10:00 - 16:00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-300">Dimanche</span>
              <span class="text-red-400 font-semibold">Fermé</span>
            </div>
          </div>
          
          <div class="mt-6 p-4 bg-green-500/20 border border-green-500/30 rounded-lg">
            <p class="text-green-300 text-sm">
              ⚡ <strong>Réponse rapide :</strong> Nous répondons à tous les emails sous 2h en journée !
            </p>
          </div>
        </div>

        <!-- Guarantees -->
        <div class="bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl p-8 border border-gray-700">
          <h3 class="text-2xl font-bold mb-6 text-pink-400">🛡️ Nos Garanties</h3>
          
          <div class="space-y-4">
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-green-400 rounded-full"></div>
              <span class="text-gray-300">Devis gratuit sous 24h</span>
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-blue-400 rounded-full"></div>
              <span class="text-gray-300">Mockup 3D offert</span>
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-purple-400 rounded-full"></div>
              <span class="text-gray-300">Fabrication française</span>
            </div>
            <div class="flex items-center gap-3">
              <div class="w-2 h-2 bg-yellow-400 rounded-full"></div>
              <span class="text-gray-300">Garantie 2 ans</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- SEO Meta Tags -->
<script>
document.title = '{{ page_title }}';
if (document.querySelector('meta[name="description"]')) {
  document.querySelector('meta[name="description"]').setAttribute('content', '{{ page_description }}');
}
</script>