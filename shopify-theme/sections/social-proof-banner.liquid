{% comment %}
  Section Social Proof Banner - Bandeau de preuve sociale
{% endcomment %}

<div class="bg-gradient-to-r from-green-500/20 to-emerald-500/20 border-y border-green-500/30 py-4 px-4 relative overflow-hidden">
  <!-- Animated Background -->
  <div class="absolute inset-0 bg-gradient-to-r from-transparent via-green-500/5 to-transparent animate-pulse"></div>
  
  <div class="relative z-10 max-w-7xl mx-auto">
    <div class="flex flex-wrap justify-center items-center gap-6 lg:gap-12">
      
      <!-- Live Counter -->
      <div class="flex items-center gap-2 text-green-300">
        <div class="w-3 h-3 bg-green-400 rounded-full animate-pulse"></div>
        <span class="font-semibold">🔥 <span id="live-counter">{{ section.settings.base_count | default: 127 }}</span> clients satisfaits ce mois</span>
      </div>
      
      <!-- Recent Activity -->
      <div class="flex items-center gap-2 text-blue-300">
        <svg class="w-4 h-4 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
        </svg>
        <span class="font-medium">⚡ <span id="recent-activity">{{ section.settings.recent_text | default: '3 devis envoyés dans les 2 dernières heures' }}</span></span>
      </div>
      
      <!-- Trust Indicator -->
      <div class="flex items-center gap-2 text-yellow-300">
        <svg class="w-4 h-4 fill-current" viewBox="0 0 20 20">
          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
        </svg>
        <span class="font-medium">⭐ {{ section.settings.rating | default: '4.8/5' }} - {{ section.settings.reviews | default: '127 avis Google' }}</span>
      </div>
      
      <!-- Guarantee -->
      <div class="flex items-center gap-2 text-purple-300">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
        </svg>
        <span class="font-medium">🛡️ {{ section.settings.guarantee | default: 'Garantie 2 ans + SAV 24/7' }}</span>
      </div>
    </div>
  </div>
</div>

<script>
// Live counter animation
document.addEventListener('DOMContentLoaded', function() {
  const counter = document.getElementById('live-counter');
  const baseCount = {{ section.settings.base_count | default: 127 }};
  
  // Simulate live updates
  setInterval(() => {
    if (Math.random() < 0.1) { // 10% chance every 5 seconds
      const currentCount = parseInt(counter.textContent);
      counter.textContent = currentCount + 1;
      
      // Flash effect
      counter.style.color = '#fbbf24';
      setTimeout(() => {
        counter.style.color = '#10b981';
      }, 500);
    }
  }, 5000);
  
  // Update recent activity text
  const activities = [
    "{{ section.settings.activity_1 | default: '3 devis envoyés dans les 2 dernières heures' }}",
    "{{ section.settings.activity_2 | default: '2 néons commandés aujourd\'hui' }}",
    "{{ section.settings.activity_3 | default: '1 installation terminée ce matin' }}",
    "{{ section.settings.activity_4 | default: '5 consultations en cours' }}"
  ];
  
  const activityElement = document.getElementById('recent-activity');
  let activityIndex = 0;
  
  setInterval(() => {
    activityIndex = (activityIndex + 1) % activities.length;
    activityElement.style.opacity = '0';
    setTimeout(() => {
      activityElement.textContent = activities[activityIndex];
      activityElement.style.opacity = '1';
    }, 300);
  }, 8000);
});
</script>

{% schema %}
{
  "name": "Bandeau Preuve Sociale",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_banner",
      "label": "Afficher le bandeau",
      "default": true
    },
    {
      "type": "number",
      "id": "base_count",
      "label": "Nombre de clients de base",
      "default": 127
    },
    {
      "type": "text",
      "id": "recent_text",
      "label": "Texte activité récente",
      "default": "3 devis envoyés dans les 2 dernières heures"
    },
    {
      "type": "text",
      "id": "activity_1",
      "label": "Activité 1",
      "default": "3 devis envoyés dans les 2 dernières heures"
    },
    {
      "type": "text",
      "id": "activity_2",
      "label": "Activité 2",
      "default": "2 néons commandés aujourd'hui"
    },
    {
      "type": "text",
      "id": "activity_3",
      "label": "Activité 3",
      "default": "1 installation terminée ce matin"
    },
    {
      "type": "text",
      "id": "activity_4",
      "label": "Activité 4",
      "default": "5 consultations en cours"
    },
    {
      "type": "text",
      "id": "rating",
      "label": "Note",
      "default": "4.8/5"
    },
    {
      "type": "text",
      "id": "reviews",
      "label": "Nombre d'avis",
      "default": "127 avis Google"
    },
    {
      "type": "text",
      "id": "guarantee",
      "label": "Garantie",
      "default": "Garantie 2 ans + SAV 24/7"
    }
  ],
  "presets": [
    {
      "name": "Bandeau Preuve Sociale"
    }
  ]
}
{% endschema %}