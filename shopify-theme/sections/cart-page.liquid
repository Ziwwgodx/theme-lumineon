{% comment %}
  Section Cart Page - Page panier avec responsive mobile optimisé
{% endcomment %}

<style>
/* Cart Page Styles */
.cart-page {
  min-height: 100vh;
  background: linear-gradient(135deg, #111827 0%, #1f2937 50%, #111827 100%);
  padding: 2rem 0;
}

.cart-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
}

/* Cart Header */
.cart-header {
  text-align: center;
  margin-bottom: 3rem;
}

.cart-title {
  font-size: clamp(2rem, 6vw, 4rem);
  font-weight: 900;
  margin-bottom: 1rem;
  background: linear-gradient(45deg, #00f5ff, #a855f7, #ec4899);
  background-size: 400% 400%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: gradient-shift 6s ease-in-out infinite;
}

@keyframes gradient-shift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

.cart-subtitle {
  color: #d1d5db;
  font-size: 1.1rem;
  max-width: 600px;
  margin: 0 auto;
}

/* Cart Items */
.cart-items {
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.9), rgba(55, 65, 81, 0.7));
  border-radius: 20px;
  border: 1px solid rgba(0, 245, 255, 0.2);
  backdrop-filter: blur(15px);
  overflow: hidden;
  margin-bottom: 2rem;
}

.cart-item {
  display: flex;
  align-items: center;
  padding: 1.5rem;
  border-bottom: 1px solid rgba(75, 85, 99, 0.3);
  transition: all 0.3s ease;
  position: relative;
}

.cart-item:last-child {
  border-bottom: none;
}

.cart-item:hover {
  background: rgba(0, 245, 255, 0.05);
}

/* Mobile Layout */
@media (max-width: 768px) {
  .cart-item {
    flex-direction: column;
    align-items: stretch;
    padding: 1rem;
    gap: 1rem;
  }
  
  .cart-item-image-container {
    align-self: center;
  }
  
  .cart-item-details {
    text-align: center;
  }
  
  .cart-item-controls {
    justify-content: center;
    flex-wrap: wrap;
    gap: 1rem;
  }
}

/* Desktop Layout */
@media (min-width: 769px) {
  .cart-item {
    gap: 1.5rem;
  }
  
  .cart-item-details {
    flex: 1;
  }
  
  .cart-item-controls {
    align-items: center;
    gap: 1rem;
  }
}

.cart-item-image {
  width: 80px;
  height: 80px;
  border-radius: 12px;
  object-fit: cover;
  border: 2px solid rgba(0, 245, 255, 0.3);
  transition: all 0.3s ease;
}

.cart-item-image:hover {
  border-color: #00f5ff;
  box-shadow: 0 0 20px rgba(0, 245, 255, 0.3);
}

.cart-item-title {
  color: #ffffff;
  font-weight: 700;
  font-size: 1.1rem;
  margin-bottom: 0.5rem;
  line-height: 1.3;
}

.cart-item-variant {
  color: #9ca3af;
  font-size: 0.9rem;
  margin-bottom: 0.5rem;
}

.cart-item-price {
  color: #00f5ff;
  font-weight: bold;
  font-size: 1.1rem;
  text-shadow: 0 0 10px rgba(0, 245, 255, 0.3);
}

.cart-item-controls {
  display: flex;
  align-items: center;
}

/* Quantity Controls */
.quantity-controls {
  display: flex;
  align-items: center;
  background: rgba(31, 41, 55, 0.8);
  border: 1px solid rgba(75, 85, 99, 0.5);
  border-radius: 12px;
  overflow: hidden;
}

.quantity-btn {
  width: 40px;
  height: 40px;
  background: transparent;
  border: none;
  color: #00f5ff;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
  font-size: 1.2rem;
  font-weight: bold;
}

.quantity-btn:hover {
  background: rgba(0, 245, 255, 0.1);
  color: #ffffff;
}

.quantity-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.quantity-input {
  width: 60px;
  height: 40px;
  background: transparent;
  border: none;
  color: #ffffff;
  text-align: center;
  font-weight: 600;
  font-size: 1rem;
}

.quantity-input:focus {
  outline: none;
  background: rgba(0, 245, 255, 0.1);
}

/* Remove Button */
.remove-btn {
  width: 40px;
  height: 40px;
  background: linear-gradient(45deg, #ef4444, #dc2626);
  border: none;
  border-radius: 50%;
  color: white;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
  margin-left: 1rem;
}

.remove-btn:hover {
  transform: scale(1.1) rotate(90deg);
  box-shadow: 0 0 20px rgba(239, 68, 68, 0.5);
}

/* Mobile Remove Button */
@media (max-width: 768px) {
  .remove-btn {
    margin-left: 0;
    margin-top: 0.5rem;
  }
}

/* Cart Summary */
.cart-summary {
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.9), rgba(55, 65, 81, 0.7));
  border-radius: 20px;
  border: 1px solid rgba(0, 245, 255, 0.2);
  backdrop-filter: blur(15px);
  padding: 2rem;
  position: sticky;
  top: 2rem;
}

.cart-total-line {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 0;
  border-bottom: 1px solid rgba(75, 85, 99, 0.3);
}

.cart-total-line:last-child {
  border-bottom: none;
  font-size: 1.2rem;
  font-weight: bold;
}

.cart-total-amount {
  color: #00f5ff;
  font-weight: bold;
  text-shadow: 0 0 10px rgba(0, 245, 255, 0.3);
}

/* Checkout Button */
.checkout-btn {
  width: 100%;
  padding: 1.2rem;
  background: linear-gradient(45deg, #00f5ff, #a855f7);
  border: none;
  border-radius: 15px;
  color: white;
  font-weight: bold;
  font-size: 1.1rem;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-top: 1.5rem;
  text-decoration: none;
  display: block;
  text-align: center;
}

.checkout-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 30px rgba(0, 245, 255, 0.4);
}

/* Continue Shopping */
.continue-shopping {
  width: 100%;
  padding: 1rem;
  background: transparent;
  border: 2px solid rgba(0, 245, 255, 0.3);
  border-radius: 15px;
  color: #00f5ff;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-top: 1rem;
  text-decoration: none;
  display: block;
  text-align: center;
}

.continue-shopping:hover {
  background: rgba(0, 245, 255, 0.1);
  border-color: #00f5ff;
}

/* Empty Cart */
.empty-cart {
  text-align: center;
  padding: 4rem 2rem;
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.9), rgba(55, 65, 81, 0.7));
  border-radius: 20px;
  border: 1px solid rgba(0, 245, 255, 0.2);
  backdrop-filter: blur(15px);
}

.empty-cart-icon {
  width: 100px;
  height: 100px;
  margin: 0 auto 2rem;
  color: rgba(0, 245, 255, 0.3);
}

.empty-cart h2 {
  color: #ffffff;
  font-size: 2rem;
  margin-bottom: 1rem;
}

.empty-cart p {
  color: #9ca3af;
  margin-bottom: 2rem;
  font-size: 1.1rem;
}

/* Trust Indicators */
.trust-indicators {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
  margin: 2rem 0;
}

.trust-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1rem;
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.8), rgba(55, 65, 81, 0.6));
  border-radius: 12px;
  border: 1px solid rgba(75, 85, 99, 0.3);
  transition: all 0.3s ease;
}

.trust-item:hover {
  border-color: rgba(0, 245, 255, 0.5);
  transform: translateY(-2px);
}

.trust-icon {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
}

.trust-text {
  flex: 1;
}

.trust-title {
  color: #ffffff;
  font-weight: 600;
  margin-bottom: 0.25rem;
}

.trust-description {
  color: #9ca3af;
  font-size: 0.9rem;
}

/* Payment Methods */
.payment-methods {
  display: flex;
  justify-content: center;
  gap: 1rem;
  margin-top: 1.5rem;
  flex-wrap: wrap;
}

.payment-method {
  width: 50px;
  height: 35px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  transition: all 0.3s ease;
}

.payment-method:hover {
  background: rgba(255, 255, 255, 0.2);
  transform: scale(1.1);
}

/* Loading States */
.loading {
  opacity: 0.6;
  pointer-events: none;
}

.loading .quantity-btn,
.loading .remove-btn {
  cursor: not-allowed;
}

/* Responsive Grid */
@media (max-width: 1024px) {
  .cart-layout {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .cart-summary {
    position: static;
  }
}

@media (max-width: 768px) {
  .cart-container {
    padding: 0 0.5rem;
  }
  
  .cart-page {
    padding: 1rem 0;
  }
  
  .trust-indicators {
    grid-template-columns: 1fr;
  }
  
  .payment-methods {
    gap: 0.5rem;
  }
  
  .payment-method {
    width: 45px;
    height: 32px;
    font-size: 1.3rem;
  }
}

/* Animation for item removal */
@keyframes item-remove {
  0% {
    opacity: 1;
    transform: translateX(0);
    max-height: 200px;
  }
  50% {
    opacity: 0;
    transform: translateX(-100px);
    max-height: 200px;
  }
  100% {
    opacity: 0;
    transform: translateX(-100px);
    max-height: 0;
    padding: 0;
    margin: 0;
  }
}

.cart-item.removing {
  animation: item-remove 0.5s ease-out forwards;
}

/* Success/Error Messages */
.cart-message {
  padding: 1rem;
  border-radius: 12px;
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-weight: 600;
}

.cart-message.success {
  background: rgba(16, 185, 129, 0.2);
  border: 1px solid rgba(16, 185, 129, 0.3);
  color: #10b981;
}

.cart-message.error {
  background: rgba(239, 68, 68, 0.2);
  border: 1px solid rgba(239, 68, 68, 0.3);
  color: #ef4444;
}

/* Shipping Progress */
.shipping-progress {
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.8), rgba(55, 65, 81, 0.6));
  border-radius: 15px;
  padding: 1.5rem;
  margin-bottom: 2rem;
  border: 1px solid rgba(16, 185, 129, 0.3);
}

.progress-bar {
  width: 100%;
  height: 8px;
  background: rgba(75, 85, 99, 0.5);
  border-radius: 4px;
  overflow: hidden;
  margin: 1rem 0;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #10b981, #059669);
  border-radius: 4px;
  transition: width 0.5s ease;
}

/* Promo Code */
.promo-code {
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.8), rgba(55, 65, 81, 0.6));
  border-radius: 15px;
  padding: 1.5rem;
  margin-bottom: 2rem;
  border: 1px solid rgba(168, 85, 247, 0.3);
}

.promo-input {
  width: 100%;
  padding: 0.8rem 1rem;
  background: rgba(55, 65, 81, 0.8);
  border: 1px solid rgba(75, 85, 99, 0.5);
  border-radius: 8px;
  color: #ffffff;
  margin-bottom: 1rem;
}

.promo-input:focus {
  outline: none;
  border-color: #a855f7;
  box-shadow: 0 0 10px rgba(168, 85, 247, 0.3);
}

.promo-btn {
  width: 100%;
  padding: 0.8rem;
  background: linear-gradient(45deg, #a855f7, #9333ea);
  border: none;
  border-radius: 8px;
  color: white;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
}

.promo-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(168, 85, 247, 0.3);
}
</style>

<div class="cart-page">
  <div class="cart-container">
    
    <!-- Breadcrumb -->
    <nav class="mb-8" aria-label="Fil d'Ariane">
      <ol class="flex items-center space-x-2 text-sm text-gray-400">
        <li><a href="/" class="hover:text-cyan-400 transition-colors">Accueil</a></li>
        <li><span class="mx-2">/</span></li>
        <li class="text-white">Panier</li>
      </ol>
    </nav>

    <!-- Header -->
    <div class="cart-header">
      <h1 class="cart-title">{{ section.settings.title | default: 'Votre Panier Néon' }}</h1>
      <p class="cart-subtitle">
        {{ section.settings.subtitle | default: '🛒 Finalisez votre commande de néons LED personnalisés. Fabrication française premium, livraison express sécurisée.' }}
      </p>
    </div>

    <!-- Messages -->
    <div id="cart-messages"></div>

    <!-- Cart Content -->
    <div class="cart-layout" style="display: grid; grid-template-columns: 1fr 400px; gap: 3rem;">
      
      <!-- Cart Items -->
      <div>
        <!-- Shipping Progress -->
        {% if section.settings.show_shipping_progress %}
          <div class="shipping-progress">
            <div class="flex justify-between items-center mb-2">
              <span class="text-white font-semibold">🚚 Livraison Gratuite</span>
              <span class="text-green-400 font-bold" id="shipping-remaining">Ajoutez {{ section.settings.shipping_threshold | default: 10000 | minus: cart.total_price | money }} pour la livraison gratuite</span>
            </div>
            <div class="progress-bar">
              <div class="progress-fill" id="shipping-progress" style="width: {{ cart.total_price | times: 100 | divided_by: section.settings.shipping_threshold | default: 10000 | at_most: 100 }}%"></div>
            </div>
            <p class="text-gray-300 text-sm">
              {% if cart.total_price >= section.settings.shipping_threshold %}
                ✅ Félicitations ! Livraison gratuite activée
              {% else %}
                Plus que {{ section.settings.shipping_threshold | default: 10000 | minus: cart.total_price | money }} pour la livraison gratuite
              {% endif %}
            </p>
          </div>
        {% endif %}

        <!-- Cart Items Container -->
        <div class="cart-items" id="cart-items">
          {% if cart.item_count > 0 %}
            {% for item in cart.items %}
              <div class="cart-item" data-key="{{ item.key }}" data-line="{{ forloop.index }}">
                
                <!-- Image -->
                <div class="cart-item-image-container">
                  {% if item.image %}
                    <img 
                      src="{{ item.image | img_url: '160x160' }}" 
                      alt="{{ item.product.title | escape }}"
                      class="cart-item-image"
                      loading="lazy"
                    />
                  {% else %}
                    <div class="cart-item-image bg-gray-700 flex items-center justify-center">
                      <svg class="w-8 h-8 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                      </svg>
                    </div>
                  {% endif %}
                </div>

                <!-- Details -->
                <div class="cart-item-details">
                  <h3 class="cart-item-title">{{ item.product.title }}</h3>
                  {% if item.variant.title != 'Default Title' %}
                    <p class="cart-item-variant">{{ item.variant.title }}</p>
                  {% endif %}
                  <p class="cart-item-price">{{ item.final_price | money }}</p>
                </div>

                <!-- Controls -->
                <div class="cart-item-controls">
                  <!-- Quantity Controls -->
                  <div class="quantity-controls">
                    <button 
                      class="quantity-btn" 
                      onclick="updateQuantity('{{ item.key }}', {{ item.quantity | minus: 1 }})"
                      {% if item.quantity <= 1 %}disabled{% endif %}
                    >
                      −
                    </button>
                    <input 
                      type="number" 
                      class="quantity-input" 
                      value="{{ item.quantity }}" 
                      min="1"
                      onchange="updateQuantity('{{ item.key }}', this.value)"
                      data-key="{{ item.key }}"
                    />
                    <button 
                      class="quantity-btn" 
                      onclick="updateQuantity('{{ item.key }}', {{ item.quantity | plus: 1 }})"
                    >
                      +
                    </button>
                  </div>

                  <!-- Remove Button -->
                  <button 
                    class="remove-btn" 
                    onclick="removeItem('{{ item.key }}')"
                    title="Supprimer cet article"
                  >
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                    </svg>
                  </button>
                </div>
              </div>
            {% endfor %}
          {% else %}
            <!-- Empty Cart -->
            <div class="empty-cart">
              <div class="empty-cart-icon">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-2.5 5M7 13l2.5 5m0 0h8m-8 0a2 2 0 100 4 2 2 0 000-4zm8 0a2 2 0 100 4 2 2 0 000-4z"></path>
                </svg>
              </div>
              <h2>Panier Vide</h2>
              <p>Votre panier est vide. Découvrez nos créations néon exceptionnelles !</p>
              <a href="/collections/all" class="checkout-btn">
                Découvrir nos Néons
              </a>
            </div>
          {% endif %}
        </div>

        <!-- Trust Indicators -->
        <div class="trust-indicators">
          {% for block in section.blocks %}
            {% if block.type == 'trust_indicator' %}
              <div class="trust-item">
                <div class="trust-icon bg-{{ block.settings.color | default: 'green' }}-500/20">
                  {{ block.settings.icon }}
                </div>
                <div class="trust-text">
                  <div class="trust-title">{{ block.settings.title }}</div>
                  <div class="trust-description">{{ block.settings.description }}</div>
                </div>
              </div>
            {% endif %}
          {% endfor %}
        </div>
      </div>

      <!-- Cart Summary -->
      {% if cart.item_count > 0 %}
        <div class="cart-summary">
          <h3 class="text-xl font-bold text-white mb-4">Récapitulatif</h3>
          
          <!-- Promo Code -->
          {% if section.settings.show_promo_code %}
            <div class="promo-code">
              <h4 class="text-white font-semibold mb-3">Code Promo</h4>
              <input 
                type="text" 
                class="promo-input" 
                placeholder="Entrez votre code"
                id="promo-code-input"
              />
              <button class="promo-btn" onclick="applyPromoCode()">
                Appliquer
              </button>
            </div>
          {% endif %}

          <!-- Totals -->
          <div class="cart-total-line">
            <span class="text-gray-300">Sous-total ({{ cart.item_count }} article{% if cart.item_count > 1 %}s{% endif %})</span>
            <span class="text-white font-semibold">{{ cart.total_price | money }}</span>
          </div>
          
          {% if section.settings.show_tax_line %}
            <div class="cart-total-line">
              <span class="text-gray-300">TVA incluse</span>
              <span class="text-gray-400">{{ cart.total_price | times: 0.2 | money }}</span>
            </div>
          {% endif %}
          
          <div class="cart-total-line">
            <span class="text-gray-300">Livraison</span>
            <span class="text-green-400">
              {% if cart.total_price >= section.settings.shipping_threshold %}
                Gratuite
              {% else %}
                {{ 990 | money }}
              {% endif %}
            </span>
          </div>
          
          <div class="cart-total-line">
            <span class="text-white font-bold text-lg">Total</span>
            <span class="cart-total-amount text-xl">
              {% if cart.total_price >= section.settings.shipping_threshold %}
                {{ cart.total_price | money }}
              {% else %}
                {{ cart.total_price | plus: 990 | money }}
              {% endif %}
            </span>
          </div>

          <!-- Payment Methods -->
          {% if section.settings.show_payment_methods %}
            <div class="payment-methods">
              {% for block in section.blocks %}
                {% if block.type == 'payment_method' %}
                  <div class="payment-method" title="{{ block.settings.name }}">
                    {{ block.settings.icon }}
                  </div>
                {% endif %}
              {% endfor %}
            </div>
          {% endif %}

          <!-- Checkout Button -->
          <a href="/checkout" class="checkout-btn">
            <svg class="w-5 h-5 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            Finaliser la Commande
          </a>
          
          <a href="/collections/all" class="continue-shopping">
            Continuer mes Achats
          </a>
        </div>
      {% endif %}
    </div>
  </div>
</div>

<script>
// Cart functionality
let isUpdating = false;

// Update quantity
async function updateQuantity(key, newQuantity) {
  if (isUpdating) return;
  
  newQuantity = parseInt(newQuantity);
  if (newQuantity < 0) newQuantity = 0;
  
  isUpdating = true;
  const cartItem = document.querySelector(`[data-key="${key}"]`);
  cartItem.classList.add('loading');
  
  try {
    const response = await fetch('/cart/change.js', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        id: key,
        quantity: newQuantity
      })
    });
    
    if (response.ok) {
      const cart = await response.json();
      
      if (newQuantity === 0) {
        // Remove item with animation
        cartItem.classList.add('removing');
        setTimeout(() => {
          window.location.reload();
        }, 500);
      } else {
        // Update quantity input
        const quantityInput = cartItem.querySelector('.quantity-input');
        quantityInput.value = newQuantity;
        
        // Update minus button state
        const minusBtn = cartItem.querySelector('.quantity-btn');
        if (newQuantity <= 1) {
          minusBtn.disabled = true;
        } else {
          minusBtn.disabled = false;
        }
        
        // Reload page to update totals
        setTimeout(() => {
          window.location.reload();
        }, 300);
      }
      
      showMessage('Panier mis à jour', 'success');
    } else {
      throw new Error('Erreur lors de la mise à jour');
    }
  } catch (error) {
    console.error('Error updating quantity:', error);
    showMessage('Erreur lors de la mise à jour du panier', 'error');
  } finally {
    isUpdating = false;
    cartItem.classList.remove('loading');
  }
}

// Remove item
async function removeItem(key) {
  if (isUpdating) return;
  
  if (!confirm('Êtes-vous sûr de vouloir supprimer cet article ?')) {
    return;
  }
  
  isUpdating = true;
  const cartItem = document.querySelector(`[data-key="${key}"]`);
  cartItem.classList.add('loading');
  
  try {
    const response = await fetch('/cart/change.js', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        id: key,
        quantity: 0
      })
    });
    
    if (response.ok) {
      // Remove with animation
      cartItem.classList.add('removing');
      setTimeout(() => {
        window.location.reload();
      }, 500);
      
      showMessage('Article supprimé du panier', 'success');
    } else {
      throw new Error('Erreur lors de la suppression');
    }
  } catch (error) {
    console.error('Error removing item:', error);
    showMessage('Erreur lors de la suppression', 'error');
  } finally {
    isUpdating = false;
  }
}

// Apply promo code
async function applyPromoCode() {
  const promoInput = document.getElementById('promo-code-input');
  const code = promoInput.value.trim();
  
  if (!code) {
    showMessage('Veuillez entrer un code promo', 'error');
    return;
  }
  
  try {
    // Shopify discount codes are applied at checkout
    // For now, just show a message
    showMessage('Code promo sera appliqué au checkout', 'success');
  } catch (error) {
    showMessage('Code promo invalide', 'error');
  }
}

// Show message
function showMessage(text, type) {
  const messagesContainer = document.getElementById('cart-messages');
  const message = document.createElement('div');
  message.className = `cart-message ${type}`;
  
  const icon = type === 'success' 
    ? '<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>'
    : '<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>';
  
  message.innerHTML = `${icon}<span>${text}</span>`;
  messagesContainer.appendChild(message);
  
  // Auto remove after 5 seconds
  setTimeout(() => {
    if (messagesContainer.contains(message)) {
      message.style.opacity = '0';
      setTimeout(() => {
        if (messagesContainer.contains(message)) {
          messagesContainer.removeChild(message);
        }
      }, 300);
    }
  }, 5000);
}

// Update shipping progress
function updateShippingProgress() {
  const threshold = {{ section.settings.shipping_threshold | default: 10000 }};
  const currentTotal = {{ cart.total_price }};
  const progress = Math.min((currentTotal / threshold) * 100, 100);
  
  const progressBar = document.getElementById('shipping-progress');
  const remainingText = document.getElementById('shipping-remaining');
  
  if (progressBar) {
    progressBar.style.width = progress + '%';
  }
  
  if (remainingText) {
    if (currentTotal >= threshold) {
      remainingText.textContent = '✅ Livraison gratuite activée !';
      remainingText.className = 'text-green-400 font-bold';
    } else {
      const remaining = threshold - currentTotal;
      remainingText.textContent = `Ajoutez ${new Intl.NumberFormat('fr-FR', {style: 'currency', currency: 'EUR'}).format(remaining / 100)} pour la livraison gratuite`;
    }
  }
}

// Initialize
document.addEventListener('DOMContentLoaded', function() {
  updateShippingProgress();
  
  // Handle quantity input changes
  document.querySelectorAll('.quantity-input').forEach(input => {
    input.addEventListener('blur', function() {
      const key = this.dataset.key;
      const newQuantity = parseInt(this.value);
      if (newQuantity !== parseInt(this.defaultValue)) {
        updateQuantity(key, newQuantity);
      }
    });
  });
});

// Handle form submissions
document.addEventListener('submit', function(e) {
  if (e.target.matches('form[action="/cart"]')) {
    e.preventDefault();
    // Handle cart updates
  }
});
</script>

{% schema %}
{
  "name": "Page Panier",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Titre de la page",
      "default": "Votre Panier Néon"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "🛒 Finalisez votre commande de néons LED personnalisés. Fabrication française premium, livraison express sécurisée."
    },
    {
      "type": "header",
      "content": "Livraison"
    },
    {
      "type": "checkbox",
      "id": "show_shipping_progress",
      "label": "Afficher la barre de progression livraison",
      "default": true
    },
    {
      "type": "number",
      "id": "shipping_threshold",
      "label": "Seuil livraison gratuite (en centimes)",
      "default": 10000,
      "info": "10000 = 100€"
    },
    {
      "type": "header",
      "content": "Options"
    },
    {
      "type": "checkbox",
      "id": "show_promo_code",
      "label": "Afficher le champ code promo",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_tax_line",
      "label": "Afficher la ligne TVA",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_payment_methods",
      "label": "Afficher les moyens de paiement",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "trust_indicator",
      "name": "Indicateur de confiance",
      "settings": [
        {
          "type": "html",
          "id": "icon",
          "label": "Icône",
          "default": "<svg class=\"w-6 h-6\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z\"></path></svg>"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Titre",
          "default": "Paiement Sécurisé"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "SSL 256-bit + Cryptage bancaire"
        },
        {
          "type": "select",
          "id": "color",
          "label": "Couleur",
          "options": [
            {"value": "green", "label": "Vert"},
            {"value": "blue", "label": "Bleu"},
            {"value": "purple", "label": "Violet"},
            {"value": "yellow", "label": "Jaune"}
          ],
          "default": "green"
        }
      ]
    },
    {
      "type": "payment_method",
      "name": "Moyen de paiement",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icône (emoji)",
          "default": "💳"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Nom",
          "default": "Visa"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Page Panier",
      "category": "Cart",
      "blocks": [
        {
          "type": "trust_indicator",
          "settings": {
            "icon": "<svg class=\"w-6 h-6\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z\"></path></svg>",
            "title": "Paiement Sécurisé",
            "description": "SSL 256-bit + Cryptage bancaire",
            "color": "green"
          }
        },
        {
          "type": "trust_indicator",
          "settings": {
            "icon": "<svg class=\"w-6 h-6\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4\"></path></svg>",
            "title": "Livraison Express",
            "description": "2-3 jours partout en France",
            "color": "blue"
          }
        },
        {
          "type": "trust_indicator",
          "settings": {
            "icon": "<svg class=\"w-6 h-6\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z\"></path></svg>",
            "title": "Garantie 2 Ans",
            "description": "Pièces et main d'œuvre incluses",
            "color": "purple"
          }
        },
        {
          "type": "trust_indicator",
          "settings": {
            "icon": "<svg class=\"w-6 h-6\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z\"></path><path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M15 11a3 3 0 11-6 0 3 3 0 016 0z\"></path></svg>",
            "title": "Made in France",
            "description": "Fabrication artisanale française",
            "color": "yellow"
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "icon": "💳",
            "name": "Visa"
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "icon": "💳",
            "name": "Mastercard"
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "icon": "🅿️",
            "name": "PayPal"
          }
        },
        {
          "type": "payment_method",
          "settings": {
            "icon": "🏦",
            "name": "Virement"
          }
        }
      ],
      "settings": {
        "title": "Votre Panier Néon",
        "subtitle": "🛒 Finalisez votre commande de néons LED personnalisés. Fabrication française premium, livraison express sécurisée.",
        "show_shipping_progress": true,
        "shipping_threshold": 10000,
        "show_promo_code": true,
        "show_tax_line": true,
        "show_payment_methods": true
      }
    }
  ]
}
{% endschema %}